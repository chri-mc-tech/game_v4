-DVCPKG_TARGET_TRIPLET=x64-mingw-static

pc fisso
-DCMAKE_TOOLCHAIN_FILE=C:\Users\matte\.vcpkg-clion\scripts\buildsystems\vcpkg.cmake

pc portatile
-DCMAKE_TOOLCHAIN_FILE=C:\Users\chrim\.vcpkg-clion\vcpkg\scripts\buildsystems\vcpkg.cmake

------------------
LOGICA PRINCIPALE
------------------

Appena un player apre il client viene generato un uuid casuale e salvato in un file di config
tutti i dati nei server sono collegati al uuid, cosi che un player puo cambiare nome senza problemi

Controlli importanti come password e crittografia farli lato server per evitare exploit il piu possibile
fare che quando un player si collega non viene fatto nulla dal server ma il client manda un pacchetto con uuid
viene creato il player con uuid come PK (parametro primario) e viene generata chiave privata server, chiave pubblica server
poi il client genera la chiave privata e pubblica del client e vengono scambiate le chiavi pubbliche
poi vengono generate le session key e dopo le encryption key sia su client che su server

Avendo gia l'uuid, il server controlla se è salvato nel db locale
se l'utente non esiste il server chiede la password per registrarsi e la salva nel db
senno chiede la pass di login e la confronta con quella salvata nel db
il server controlla l'hash col file dell'utente


---------------------
CODICI ERRORE CLIENT
---------------------

1 = general error
3 = unable to initialize libraries
4 = error creating config file
5 = error loading config
6 = error creating enet host
8 = uuid not valid


---------------------
CODICI ERRORE SERVER
---------------------

1 = general error
3 = unable to initialize libraries
4 = error creating config file
5 = error loading config
6 = error creating enet host


---------------------
CANALI
---------------------

0 = pacchetti iniziali (prima di Diffie Hellman) (non cifrati)
1 = pacchetti non cifrati (es: coordinate)
2 = pacchetti cifrati (es: hash password)



// TEMP

Trasforma coords in int nei pacchetti
Separare "uuid x y;uuid2 x2 y2;uuid3 x3 y3" ecc



---------------------
BUG DA FIXARE
---------------------

Nel client se tengo/muovo col mouse la finestra per piu di qualche secondo smette di eseguire il polling di enet (e di mandare pacchetti) e quindi si scollega dal server

Il server crasha se mi collego con 2 client con stesso uuid e nome (problema puo essere nel controllo uuid o nel controllo nome)

Fixare crashano tutti i client al register di uno

a caso quando entra uno, prima del login, crashano gli altri client

!! IMPORTANTE !!
Se un client si scollega devo azzerare la lista player per il client

fixare memory leak server, probabilmente pacchetti vengono creati ma non distrutti se non vengono inviati

server crasha a client disconnect (forse fixato)

---------------------
COSE DA AGGIUNGERE
---------------------

Modificare status del player nel client e fare:
chat aperta? bool
stato connessione (connecting, encrypting, encrypted)
stato menu

Stati come autenticato ecc fare solo lato server per sicurezza

-------

Modificare tutti i enet_peer_disconnect che ci sono nel server verso i client
aggiungendo un motivo, che poi viene letto dal client